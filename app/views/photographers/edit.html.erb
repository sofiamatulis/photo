

<h1>Edit <%= @photographer.name %></h1>

<div class="photographer-form">
<%= render 'form', photographer: @photographer %>

<div class="actions">
<%= link_to "Delete Photographer", photographer_path, method: :delete, data: {confirm: "Are you sure you want to delete this photographer?" } %>
</div>

<div class="actions">
<%= link_to 'Back', photographers_path %>
</div>

</div>
